# Paste these into the Streamlit Cloud Secrets box (Settings -> Secrets).
# Locally, use a .env file beside Entropy_app.py instead.
GROQ_API_KEY = "gsk_your_key_here"

# Optional. Omit entirely to let the app pick the best model your key can reach.
# GROQ_MODEL = "openai/gpt-oss-120b"
